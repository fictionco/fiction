describe("site-wrapper", () => {
  it("renders", () => {})

  it("allows for prefetch hooks", () => {})

  it("adds scroll class", () => {})

  it("allows for reactive classes", () => {})

  it("adds ui class", () => {})
})
