module.exports = Factor => {
  return new (class {
    constructor() {
      // Factor.$filters.add("cli-setup", (_, program) => {
      //   return [..._, require("./setup")(Factor).runSetup()]
      // })
    }
  })()
}
