describe("database", () => {
  describe("connection/instance", () => {
    it.todo("connects to mongodb server")
    it.todo("connects on appropriate hook")
    it.todo("disconnects mongodb server")
    it.todo("disconnects on appropriate hook")
  })
  describe("MongoDB indexes", () => {
    it.todo("makes sure indexes stay in sync with schemas")
  })
  describe("schemas/models", () => {
    it.todo("loads base post schema")
    it.todo("loads extension and module schemas")
    it.todo("creates models from schemas")
    it.todo("fires schema callback")
  })
})
