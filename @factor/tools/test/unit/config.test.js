describe("app config", () => {
  it.todo("adds package.json information to config")
  it.todo("adds factor-config information to config")
  it.todo("passes needed config information to client app")
})
