describe("general utilities", () => {
  describe("dot setting", () => {
    it.todo("gets values from object based on dot notation")
    it.todo("returns undefined if value isn't set")
    it.todo("allows keys with dots in them, like emails")
  })
})
