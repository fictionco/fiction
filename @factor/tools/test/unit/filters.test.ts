describe("filters", () => {
  it.todo("it adds filter hooks based on filter ID")
  it.todo("it applies all filter hooks correctly")
  it.todo("it adds push and register helpers")
  it.todo("provides a utility to output filter count")
})
