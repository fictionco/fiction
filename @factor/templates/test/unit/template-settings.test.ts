describe("template settings", () => {
  it.todo("adds UI for page management")
  it.todo("adds an API for template settings")
  it.todo("saves selected page template and loads its options")

  it.todo("adds editing UI to page posts in dash board")
  it.todo("edits and saves settings correctly")
})
