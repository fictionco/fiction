describe("cli", () => {
  describe("setup", () => {
    it("setup: run yarn install to verify node_modules installed", () => {})
    it("setup: build loaders before extension", () => {})
    it("setup: sets environmental variables", () => {})
    it("setup: support aliases '~' & '@' ", () => {})
  })

  describe("commands", () => {
    it("Runs development server with 'factor dev'", () => {})
    it("Builds and then runs production server with 'factor start'", () => {})
    it("Builds 'factor build'", () => {})
    it("Runs setup utility with 'factor setup'", () => {})
    it("Builds extension loaders with 'factor create-loaders'", () => {})
  })

  describe("logging", () => {
    it("logs dev information", () => {})
    it("logs port", () => {})
  })
})
