describe("server-restart", () => {
  it("restarts when needed", () => {})
  it("reloads transpiler and aliases on restart", () => {})
  it("reloads all filters on server restart", () => {})
  it("adds restart hook back after cache clear", () => {})
})
