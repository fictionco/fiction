<template>
  <div>
    <p class="date">{{ $time.niceFormat(post.date) }}</p>
  </div>
</template>
<script>
export default {
  props: {
    postId: { type: String, default: "" }
  },
  computed: {
    post() {
      return this.$store.val(this.postId) || {}
    }
  }
}
</script>
