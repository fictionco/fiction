describe("endpoint", () => {
  describe("server", () => {
    it("adds endpoint middleware from endpoint filter", () => {})
    it("parses JWT bearer token into user", () => {})
    it("returns JSON result and error in response", () => {})
    it("elegantly handles endpoint errors on server", () => {})
    it("runs endpoint methods based on module or class handler", () => {})
    it("passes correct parameters", () => {})
  })

  describe("app/browser", () => {
    it("allows requests from browser/app", () => {})
    it("adds bearer header", () => {})
    it("uses POST request", () => {})
    it("elegantly handles endpoint errors in browser", () => {})
  })
})
