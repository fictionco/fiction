{
  "name": "<%= urlName %>",
  "version": "1.0.0",
  "homepage": "<%= url %>",
  "description": "<%= description %>",
  "author": "<%= author %>",
  "private": true,
  "scripts": {},
  "dependencies": {
    "@factor/<%= scope %>": ">=0.0.7"<% if (scope != 'build') { %>,
    "@factor/stack-fire": ">=0.0.7"<% } %>
  }
}
